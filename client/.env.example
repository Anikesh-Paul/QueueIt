# Client (copy to .env and adjust). Never commit real secrets.
# Base URL of the QueueIt Express API (no trailing slash).
# Local:  http://localhost:5000
# Production (Vercel env at build time): https://your-api.vercel.app
VITE_API_URL=http://localhost:5000
